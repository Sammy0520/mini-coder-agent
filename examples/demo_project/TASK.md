# Demo task

The discount module has a boundary-checking bug. Diagnose it from the tests, make the smallest correct change, run the tests, and report the result.

Expected verification command:

```text
python -m unittest -v
```
